# Cambia esta URL si tu servidor de Symfony corre en un puerto distinto (ej: 8001)
VITE_BACKEND_URL=http://localhost:8000
# En Vite, todas las variables deben empezar por VITE_ para que React las pueda leer